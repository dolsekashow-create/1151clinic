# dependencies
node_modules/
.pnpm-store/

# build output
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo
next-env.d.ts

# مجلد عمل مؤقت (قاعدة بيانات الاختبار، سجلات التشغيل)
.tmp/

# env & secrets  (NEVER commit real credentials)
.env
.env.*
!.env.example
*.pem
*.key

# supabase local
supabase/.branches/
supabase/.temp/
supabase/.env

# testing
coverage/
.vitest/
playwright-report/
test-results/

# editors / os
.vscode/*
!.vscode/extensions.json
!.vscode/settings.recommended.json
.idea/
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# vercel
.vercel

# اعتمادات مستخدمي التجربة — ممنوع رفعها
.demo-credentials/

# ── أسرار واعتمادات: طبقة حماية إضافية صريحة ──
# (.env.* مُغطّاة أعلاه؛ هذه أنماط شائعة يسهل نسيانها)
*.env
.env.local
.env.*.local
secrets/
*.secret
*.secrets
service-account*.json
*credentials*.json
!.demo-credentials/.gitkeep

# مفاتيح ونسخ قواعد بيانات
*.p12
*.pfx
*.jks
*.sql.gz
*.dump
dump.sql
backup*.sql
