-- =============================================================================
--  Migration : 20260817210000_service_role_app_usage
--  Phase     : 7 (إصلاح) — منح service_role وصولًا لمخطط app
--
--  الخلل المكتشَف بالتجربة الفعلية:
--
--    `public.consume_rate_limit` غلاف `security invoker` يستدعي
--    `app.consume_rate_limit`. مُنح `service_role` صلاحية EXECUTE على
--    الاثنين — لكنه **لا يملك USAGE على مخطط `app`** أصلًا، والمنح على دالة
--    داخل مخطط لا يُرى لا قيمة له.
--
--    النتيجة: كل نداء للعدّاد يفشل بـ«permission denied for schema app»،
--    وطبقة التطبيق تلتقط الخطأ وتُمرّر الطلب (فشل مفتوح مقصود وموثّق) — أي أن
--    **الحد من المعدّل كان معطّلًا بصمت على كل النقاط، بما فيها الحجز العام**.
--
--    لماذا لم تكشفه الاختبارات: كلها تعمل بدور `authenticated` (الذي يملك
--    USAGE لأن سياسات RLS تستدعي `app.has_permission`)، والمسار الوحيد الذي
--    يمر بـ`service_role` هو العدّاد — واختبرتُ أن `anon` **يُرفض** منه، ولم
--    أختبر أن الخادم **ينجح** فيه. الاختبار السلبي وحده لا يكفي.
--
--  ⚠️ المنح هنا USAGE على المخطط فقط. لا يُمنح `service_role` تنفيذ أي دالة
--     لم تُمنح له صراحةً — و`alter default privileges` في ترحيل التقوية يمنع
--     المنح التلقائي لأي دالة جديدة.
-- =============================================================================

grant usage on schema app to service_role;

-- إعادة تأكيد المنح الصريح بعد إتاحة المخطط
grant execute on function app.consume_rate_limit(text, integer, integer) to service_role;

comment on schema app is
  'دوال الأمان والمنطق. مكشوف لـ authenticated (تستدعيه سياسات RLS) و '
  'service_role (يستدعي عدّاد الحد من المعدّل). غير مكشوف لـ anon ولا لـ PUBLIC.';
